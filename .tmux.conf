# run-shell "powerline-daemon -q"
# source "/usr/lib/python2.7/site-packages/powerline/bindings/tmux/powerline.conf"

unbind  C-b
bind    C-a send-prefix
set -g prefix C-a

set-option -g allow-rename off

bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

bind C-k resizep -U 10
bind C-j resizep -D 10
bind C-h resizep -L 10
bind C-l resizep -R 10

bind C-u swapp -U       # swap with upper panel
bind C-d swapp -D       # swap with lower panel


# set -g base-index 1
# 
# new-session
# 
# set -s escape-time 0
# 
# set -g visual-activity on
set -g history-limit 50000
# #set -g lock-after-time 300
# 
# # Set status bar
# set -g status-interval 5
# set -g status-bg black
# set -g status-fg white
# set -g status-left-length 50
# set -g status-left '#[fg=green][#(hostname -s)]'
# set -g status-right-length 50
# set -g status-right '#[fg=yellow]#(uptime | cut -d "," -f 1,3- | sed -e "s/up[a-z0-9, :]*: /\[/g")]'
# 
# # Set window options
# setw -g utf8 on
# setw -g mode-keys vi
# setw -g window-status-current-bg red
# setw -g aggressive-resize on
# setw -g monitor-activity on
# setw -g automatic-rename on


